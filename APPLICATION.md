# MoonBit 黑客松项目申请书

## 1、项目名称

**MoonLayout：MoonBit 原生的确定性图自动布局引擎**

## 2、项目简介

MoonLayout 是一个使用 MoonBit 编写的轻量级图自动布局库和 SVG 生成工具。用户
可以用简单的边列表（例如 `Client-API,API-DB,API-Queue`）描述节点之间的关系，
MoonLayout 会计算节点坐标、保留连线关系，并生成可以直接嵌入网页、文档或报告的
自包含 SVG 图像。

项目的核心特点是**确定性、无网络依赖、可复现和易嵌入**：布局使用整数运算、固定
初始网格和明确的参数约束，不依赖随机数或外部服务。同一份输入和配置会产生相同的
坐标与 SVG 内容，方便测试、文档构建、CI 检查和结果审阅。核心 API 与 CLI 分离，
后续可以在 JavaScript、WASM 或其他 MoonBit 应用中复用。

## 3、项目方向与通用性说明

### 项目方向

项目属于赛事“应用开发与内容处理”方向中的**自动布局引擎/通用可视化组件**，与
`d3-force` 一类的图布局能力相近。项目不属于编程语言实现、编辑器、LSP 或传统命令
行工具开发，也不依赖特定行业后端服务。

### 通用性说明

图结构是很多软件系统的共同表达方式：节点可以代表服务、模块、任务、人员、页面或
知识点，边可以代表依赖、调用、流程、关联或引用。MoonLayout 将“图数据建模—布局
计算—SVG 输出”拆成通用接口，不绑定数据库、前端框架或业务协议，因此可以作为：

- MoonBit 文档工具的依赖关系图组件；
- Web 应用中的流程图、架构图和关系图生成模块；
- 教学、报告和静态站点中的可复现图形渲染组件；
- WASM 应用或 CI 流程中的离线图布局能力。

当前实现优先覆盖小型和中型图，强调稳定、易测试和可移植；后续可以在不改变基础
数据模型的前提下扩展箭头、标签、连通分量打包和增量布局。

## 4、完整的预期使用场景

### 场景一：软件系统架构图生成

开发者在项目说明中维护服务关系：

```text
Client-API,API-Auth,API-DB,API-Queue,Queue-Worker
```

构建脚本调用 MoonLayout 生成 `architecture.svg`，将其嵌入 README、项目文档或
静态站点。代码变更后重新运行即可得到新的架构图；由于布局是确定性的，CI 可以比较
输出差异，评审者也能复现同一张图，而不需要打开图形编辑器手工拖拽节点。

### 场景二：工作流与任务依赖可视化

课程、数据处理流程或发布流水线可以把任务写成：

```text
Design-Implement,Implement-Test,Test-Release,Design-Review,Review-Implement
```

MoonLayout 计算布局并生成 SVG，帮助团队快速查看任务先后关系、汇聚点和循环依赖。
产品或技术人员只需修改文本形式的边列表，不需要学习专门的绘图格式；生成结果可
直接放入发布说明、周报或验收材料。

### 场景三：MoonBit 包依赖和模块关系图

MoonBit 开源项目可以将包、模块或功能组件作为节点，将导入和依赖作为边，生成项目
依赖图。该图可以放在包 README 或版本发布页面中，用于新贡献者理解代码结构，也
可以在提交前作为静态产物生成。MoonLayout 不访问网络、不读取远程仓库，项目作者可以
在本地或 CI 中使用同一个命令完成生成。

### 场景四：教学与知识点关联图

教师或教程作者可以把知识点表示为节点，把“先修关系”和“相关关系”表示为边，生成
课程章节图或学习路径图。SVG 是单文件格式，适合嵌入课程网页、电子讲义和离线资料；
固定布局还可以避免每次构建后图形随机变化，保持教材截图和在线页面一致。

## 5、拟实现的核心功能

### 图数据模型

- `Node`：节点名称与整数坐标；
- `Edge`：节点索引之间的有向关系；
- `Graph`：节点集合与边集合；
- `LayoutConfig`：画布尺寸、迭代次数、连线距离、斥力、重力和阻尼参数。

### 确定性布局计算

- 使用固定网格初始化节点，避免随机种子和不可复现结果；
- 使用整数力导向模拟处理节点斥力、连线拉力和中心引力；
- 每一步将节点限制在画布边界内；
- 对非法边索引进行安全忽略，避免库调用导致崩溃；
- 对尺寸、迭代次数和力参数进行统一规范化。

### 输入与输出

- 解析 `A-B,B-C,C-A` 形式的紧凑边列表；
- 自动识别重复节点，并支持孤立节点；
- 输出包含背景、连线、圆形节点和文字标签的自包含 SVG；
- 对节点文本进行 XML 转义，避免特殊字符破坏 SVG；
- 提供 `moonlayout` CLI，支持自定义输入、输出路径、画布尺寸和迭代次数。

### 工程质量

- MoonBit 单元测试覆盖确定性、边界、空图、非法边和 SVG 输出；
- Node 集成测试执行真实 CLI，覆盖正常生成和错误参数退出码；
- CI 中执行格式检查、警告检查、测试、接口生成检查、JS 构建和 CLI 测试；
- MIT License，核心库不依赖网络、数据库或第三方运行时。

## 6、原创性、移植性与参考说明

MoonLayout 是**原创项目**，不是对某个已有项目的直接移植。核心数据结构、整数化
布局算法、边界规范化、MoonBit API、SVG 渲染器和 CLI 均由本项目独立实现。

项目在产品形态和问题建模上参考了公开的力导向图布局思想，尤其是 d3-force 的使用
场景，但没有复制 d3-force 的源代码，也没有将其作为运行时依赖。MoonLayout 为了
确定性测试和 MoonBit/WASM 可移植性，采用固定网格初始化和整数计算，这是本项目的
独立工程实现。

## 7、移植项目的参考项目、来源链接与许可证

本项目不是移植项目，因此不存在必须遵循的被移植代码来源。

为便于说明算法思想，项目参考以下公开项目的产品形态和公开文档：

- 项目：**d3-force**
- 来源链接：<https://github.com/d3/d3-force>
- 上游许可证：ISC License
- 使用方式：仅参考力导向布局的通用问题建模和使用场景，不复制源代码，不打包其
  运行时，也不改变 MoonLayout 的原创实现和 MIT 许可证。

MoonLayout 自身以 MIT License 发布，许可证全文见仓库根目录的 `LICENSE` 文件。

## 验收与复现命令

```bash
moon fmt --check
moon check --deny-warn
moon test --deny-warn
moon info
moon build --target js src/cmd/moonlayout
node --test tests/cli.integration.test.mjs
moon run src/cmd/moonlayout -- --edges Client-API,API-DB,API-Queue --out demo.svg
```

验收时打开 `demo.svg`，应能看到节点、连线和文字标签；连续运行相同命令时，输出
内容保持一致。
